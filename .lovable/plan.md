

## Rename "Routes" Tab to "Pickups"

Single change in `src/components/TopNav.tsx` — update the label text from "Routes" to "Pickups" in the navigation tabs section (around line 189). The dropdown items ("Today's Routes" and "Outbound") stay the same. This makes it clear: **Pickups** = inbound tire collection routes, **Trailers** = outbound processor moves.

