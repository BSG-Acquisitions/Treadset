

## Plan: Remove redundant trailer badge

**File: `src/components/TrailerAssignmentDropdown.tsx`**

Remove the conditional badge block (lines 83-93) that shows either the trailer number badge or "No Trailer" badge. The Select dropdown already displays the current state, making the badge redundant.

This is a simple deletion — the truck icon label and the dropdown selector remain unchanged.

